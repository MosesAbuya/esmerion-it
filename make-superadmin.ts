import { prisma } from './src/auth'; async function main() { const email = process.argv[2]; if (!email) return console.log('Pass email as argument'); await prisma.user.upsert({ where: { email }, update: { isSystemSuperadmin: true }, create: { email, isSystemSuperadmin: true } }); console.log('Done!'); } main().finally(() => console.log('Finished.'));
