import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const email = process.argv[2]; if(!email) { console.error('Please provide an email'); return; } const user = await prisma.user.upsert({ where: { email }, update: { isSystemSuperadmin: true }, create: { email, isSystemSuperadmin: true } }); console.log('Successfully made', email, 'a superadmin!'); } main().finally(() => prisma.\$disconnect\());
